import React from "react";

const Layout = ({ children }) => {
    return (
    <div style={{width: '100%', backgroundColor: '#5C8F62' }}>
    {children}
    </div>
    );
    };
export default Layout;